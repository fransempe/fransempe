{
  "name": "fsempe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd astro && npm run dev",
    "build": "cd astro && npm run build",
    "preview": "cd astro && npm run preview",
    "postinstall": "cd astro && npm install"
  }
}
